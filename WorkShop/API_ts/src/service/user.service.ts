import {Request, Response} from "express";
import config from "../config"
import axios from "axios"


export class UserService {

    //// code a fetchWeather service hear ///
    /// use axios.get Function for that ////
        /// request route : https://api.openweathermap.org/data/2.5/weather
        /// params : q = City Name & appid = config.key
    static async fetchWeather(): Promise<any> {

    }

}